#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
int main(){
    vector<int> A={1,3,6,10,15,16,17};
    sort(A.begin(),A.end());
    int l=0,ans=0;
    for(int r=0;r<A.size();r++){
        while(A[r]-A[l]>10) l++;
        ans=max(ans,r-l+1);
    }
    cout<<ans;
}
